# Core dependencies for the Avellaneda-Lee stat-arb backtest.
# Verified working on Python 3.11 with the versions below.
# The code was modernized for pandas >= 2.0 (DataFrame.append removed, yaml.load Loader).
pandas>=2.0,<3.0
numpy>=1.24,<2.0
statsmodels>=0.14
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.6
PyYAML>=6.0
tqdm>=4.60

# Optional: only needed for the parallel threshold grid-search in
# mains/optimise_trading_rules.py and src/data/hist_pca_factoring.py.
# A single backtest (run_backtest.py) does NOT require ray.
# ray>=2.0

# FinGPT regime stress filter (optional — only needed if using --regime-filter).
# Install with: pip install torch transformers peft accelerate bitsandbytes huggingface_hub datasets yfinance
# GPU (CUDA) strongly recommended; 7B model requires ~6 GB VRAM with 4-bit quantization.
# News source: FNSPID dataset (Zihan1004/FNSPID on HuggingFace) — free, 1999-2023.
# torch>=2.0
# transformers>=4.35
# peft>=0.6
# accelerate>=0.24
# bitsandbytes>=0.41
# huggingface_hub>=0.20   # for download_fnspid.py
# datasets>=2.14          # HuggingFace datasets library (primary FNSPID download method)
# pyarrow>=12.0           # chunked parquet writing for large FNSPID CSV (23 GB)
# yfinance>=0.2           # VIX download in diagnostic chart (compute_stress.py)
